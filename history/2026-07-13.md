# 2026-07-13 진행 로그

### 14:33 — 읽기전용 서브에이전트에 쓰기 작업 오배정 → 라이브락 방지 (커밋 `207c534`)
- **변경**: `src/agent.ts`(spawn_subagent 설명 + handoffToUser 라이브락 가드), `src/subagent.ts`(explore 역할 프롬프트)
  - spawn_subagent 설명: 파일 생성·저장 작업은 반드시 `code`로 위임하도록 명시(explore/review는 쓰기 불가·텍스트만 반환)
  - explore 프롬프트: 쓰기 도구 없음 명시 + 저장 필요 작업이면 "code 역할 필요" 한 줄 반환하고 반복 금지
  - 라이브락 가드: 쓰기 도구 없는 서브에이전트(explore/review)가 분석마비 시 사용자 위임(루프 유발) 대신 중단·부모 반환
- **이유**: market-research 하네스에서 "경쟁 분석→`_workspace/02_competitor_analysis.md` 저장" 작업이 `explore`(읽기전용) 서브에이전트에 위임돼, write 도구가 없어 완료 조건에 영영 도달 못 하고 사용자 핸드오프가 "계속 진행"을 반복 주입해 무한 루프(livelock). 사용자가 1번(계속)을 눌러도 능력 부재라 같은 결론 재도출.
- **결과/검증**: `npx tsc --noEmit` 통과, claude 브랜치 커밋 완료.

### (측정 마일스톤) Q8이 최상위 병목 "재현-오염" 해소 확인
- **내용**: 로컬 모델을 Gemma-4-26B **UD-Q4_K_M → UD-Q8_K_XL**로 교체 후 동일 프로브 재측정.
  - calc.js 편집(plain): Q4는 재현오염→스파이럴 실패 → **Q8 성공**(오염 0).
  - /language-tutor: Q4는 `00_input.md`를 `언구`/`レベル`/`.py`로 깨뜨리고 파라리시스로 죽음 → **Q8은 파이프라인 완주**(00~07 파일, 오염 0, 게이트 준수).
- **결론**: 최상위 실패 원인은 추론이 아니라 **재현 충실도(reproduction fidelity)**였고, Q8(비트 증가)로 해소됨. (QAT-Q4가 같은 효과를 Q4 속도로 내는지는 미측정 — 다음 과제)

### (인프라) Q8 서버 안정 구동 플래그 확정
- **내용**: 이 llama.cpp 빌드(b9144) + Gemma SWA에서 서버 반복 크래시(`common_prompt_checkpoint::load_tgt` 코어덤프).
- **해결**: `--cache-ram 0`(prompt-cache 끄기) + `--parallel 1`(단일 슬롯 직렬화)로 완전 안정화.
  - 실행: `llama-server -m .../gemma-4-26B-A4B-it-UD-Q8_K_XL.gguf --host 0.0.0.0 --port 8080 -c 32768 -ngl 99 --jinja --cache-ram 0 --parallel 1`

---

## 이전 진행 (참고)
- 2026-07-07 세션: language-tutor 하네스 버그 수정(무한 예고, 거부 우회 루프, 날조 방지 게이트, 대화형/생성 위임 규칙, 출력 언어 규칙), `/스킬명` 슬래시 직접 발동, 코드작업 자동 critic 라우팅 등. (커밋 `78b4004`, `76301a8`, `2280c92`, `5e0a335`, `6470d56`, `5052d4d`, `73786b2`, `768766e` 등 — `git log` 참조)
