# 2026-07-15 진행 로그

### 15:15 — patch_ast_node 확장: 클래스 메서드(Class.method) 편집 지원 (커밋 `bcd62ea`)
- **변경**: `src/ast.ts`(patchJsTs·PY_LOCATE·patchPython 확장), `src/tools.ts`(patch_ast_node 도구 설명·target_symbol 설명·에러 힌트 문구)
  - JS/TS: 클래스 멤버(메서드·프로퍼티·접근자·생성자)를 `Class.method`로 지정해 교체. 메서드 이름이 파일에서 유일하면 이름만으로도 매칭, 여러 곳이면 모호 에러로 안내.
  - Python: 클래스 메서드 `Class.method` 지원 + 새 본문을 들여쓰기 없이(col 0) 써도 원본 들여쓰기에 맞춰 자동 재배치.
  - 최상위 함수/클래스/const 동작은 그대로(회귀 보존).
- **이유**: 기존 AST 편집은 최상위 심볼만 가능해 실제 코드의 큰 부분인 **클래스 메서드를 못 고침**. "이름으로 가리켜 재현(old_string 베끼기)을 피하는" 검증된 편집 패러다임을 메서드까지 넓혀, 약한 모델의 재현-오염을 더 많은 편집 케이스에서 우회.
- **결과/검증**: `npx tsc --noEmit` 통과. 유닛 테스트 7/7 통과(JS Class.method·bare 이름·최상위 회귀·constructor·모호 심볼목록, Python Class.method+자동 들여쓰기·최상위 회귀).
- **맥락**: "재현 충실도가 임계 제약이고, ①도구 호출 재현은 하네스로 우회 가능"이라는 진단에서 나온 후속 작업. AST 편집 커버리지 확대 = ① 축을 더 넓게 방어.

### (측정) AST 메서드 편집 프로브 — ①/② 프레임 확증
- **설계**: 메서드 편집 과제, plain 경로, 조건 A(AST 사용)/B(MCC_ABLATE=ast로 edit_file 강제), 3회씩. Q8·Q4에서 각각.
- **결과**:
  - **단순 메서드(shape.js, 1줄 본문)**: Q8·Q4 모두 A 3/3·B 3/3 (편집매칭실패 0). → 짧은 편집은 Q4도 안 깨짐, 델타 없음.
  - **큰 메서드 재작성(an.js, ~12줄)·Q4**: A 0/3·B 0/3. 단 **메커니즘은 정반대** — A(AST): 편집매칭실패 **0**, B(edit_file): 편집매칭실패 **2~3회/실행**. 둘 다 반복루프(②)로 중단.
- **해석(확정)**: AST는 **①(old_string 재현 실패)을 제거**함(0 vs 2~3, 측정됨). 그러나 pass는 0:0 — **②(사고 중 변수명 환각→스파이럴)**가 병목이고 AST는 ②를 못 건드림. 이전 "AST 편집실패 92%→0%인데 pass rate는 그대로(반복 병목)" 현상과 동일. → **Q8 미만·복잡 편집에서 AST는 필요조건이나 충분조건 아님. ②는 모델(Q8/QAT)만 해결.** 저분산 메커니즘 지표가 고분산 pass rate보다 AST 기여를 선명히 드러냄(교훈 재확인).
- **모델별 요약**: Q8은 단순·복잡 모두 재현-오염 없음(①②둘다). Q4는 단순 OK, 복잡 편집은 ②로 실패.

### (측정·폐기) ②(반복 스파이럴) 텍스트 넛지 개입 — 미증명으로 폐기
- **시도**: REPETITION_LOOP 복구 메시지를 ② 겨냥으로 교체("파일 내용을 기억에서 재인용·이름 추측하지 마라 / 편집은 patch_ast_node로 재현 없이 / 필요하면 read_file로 다시 읽어 그대로"). `MCC_ABLATE=repfix` 토글로 A/B 가능하게 구현.
- **측정**: 큰 메서드 프로브(Q4, AST 사용), 개입 ON vs OFF 각 5회. 결과 — **ON 1/5 vs OFF 0/5**(반복이벤트 2.8 vs 3.0, 강제중단 3 vs 4). 모두 **n=5 노이즈 범위, 통계적 무의미**. (주의: 초기 pass 집계에 substring 매칭 버그로 OFF #1을 오탐 PASS 처리 → 실제 max=90.1 오염값이라 FAIL로 정정)
- **결론**: 텍스트 넛지로는 ②(모델 머릿속 저수준 토큰 오염)를 못 잡음 → **미증명. 커밋하지 않고 폐기(revert).** "②는 모델(Q8/QAT) 몫" 재확인. 폐기 이유 기록으로 재발명 방지.
- **다음 후보**: ② 실질 해결은 QAT-Q4(속도 유지) 또는 Q8. 하네스 텍스트 개입은 이 축에서 천장이 낮음.

### 15:02 — 경로 퍼지 수리 레이어 (커밋 `66c9d1c`)
- **변경**: `src/tools.ts` — safePath 뒤에 `resolveExistingPath`/`safeExisting`(+levenshtein) 추가, read_file/edit_file/patch_ast_node에 연결.
  - 요청 경로가 없으면 같은 디렉터리에서 '유일하게 근접한 실존 파일'을 찾아 대신 사용, 수리 사실을 결과에 note로 밝힘. 매칭 규칙: (a)같은 스템·다른 확장자 또는 (b)같은 확장자+편집거리≤2+이름길이≥4. 후보 유일할 때만(모호하면 미수리).
  - `MCC_ABLATE=pathfix`로 끔. 기존 editfix(old_string 공백관용)와 상보적 — 이건 '경로' 축(①의 나머지 절반).
- **이유**: language-tutor에서 관찰된 `.md→.py/.mmd` 파일명 오염이 지금까지 미처리였음(edit_file 수리는 old_string만 다룸). 모델과 무관한 결정론적 복구.
- **결과/검증**: `tsc` 통과. 유닛 테스트 5/5(확장자오염 .py→.md, 오타 calk.js→calc.js, 실존파일 미수리, 모호 미수리, patch_ast_node 경로수리+편집).
- **성격(정직)**: 이건 repfix 같은 '행동 넛지'가 아니라 **결정론적·오매칭 방어(유일매칭) 안전망**이라 유닛 테스트로 정확성 입증됨. 다만 read/edit 경로 오염의 end-to-end 발생 빈도는 미측정(대체로 낮을 것 — "보험"). pass rate 상승을 주장하지 않음.

### 13:54 — L4 힐 클라이밍: 트레이스 진단 엔진 `/engine` (커밋 `c1d740e`)
- **변경**: `src/engine.ts` 신규 + `src/index.ts`에 `/engine` 명령 연결.
  - 누적 `runs.jsonl`에서 저분산 메커니즘 지표(편집실패·파싱실패·반복루프·거부·미완주 결과)를 집계 → 반복 실패 패턴을 '증거 딸린 진단 이슈'로 심각도순 랭크. 각 이슈에 ①(하네스로 우회)/②(모델 몫) 레버 + 후보 대응. 재발 오류 지문(≥2회) 집계.
  - 결정론적 규칙 기반(evolve의 LLM 반성과 상보). eval/metrics.ts `classifyError`와 정렬.
- **이유**: 이번 세션 내내 사람이 손으로 하던 "로그 grep → 병목 진단"을 자동화 = 4루프 프레임의 **L4(트레이스 분석→이슈 등록)** 첫 절반. mcc는 L1·L2(critic) 보유, L3(이벤트) 없음, L4는 수동이었음 → 그 분석 단계를 자동화.
- **핵심 원칙(정직)**: 엔진은 **하네스를 바꾸지 않는다** — 이슈만 등록하고 "채택 전 ablation 측정하라"고 명시. repfix를 측정으로 폐기한 교훈대로, '측정 없는 자동 개선'을 의도적으로 배제. L4의 나머지 절반(자동 제안→자동 ablation 검증→자동 채택)은 후속.
- **결과/검증**: `tsc` 통과. 합성 6런으로 집계·랭크·①/② 레버 분류 확인(반복3→②, 미완주→혼합, 편집실패2→①).

### 14:46 — L4 나머지 절반: 자동 ablation 검증 루프 `eval/ablate.ts` (커밋 `35fa5ec`)
- **변경**: `eval/ablate.ts` 신규 + `eval/run.ts`에 `EVAL_OUT` 훅(결과 경로 고정).
  - 후보 개선책(ablate 토글)을 ON(기능 있음)/OFF(기능 제거) 두 조건으로 같은 과제를 N회씩 자동 실행 → 저분산 메커니즘 지표로 효과있음/없음/역효과 판정. run.ts를 두 번 spawn(프로세스 격리 = 조건별 config.ablate 정확 반영).
  - 판정: 메커니즘 실패(편집·파싱·반복)의 절대(≥2)+상대(≥30%) 감소 둘 다일 때만 '효과 있음'(보수적). 통과율은 고분산이라 큰 차이(≥0.3)일 때만 보조.
- **이유**: 이번 세션에 손으로 하던 A/B 측정(AST·repfix)을 자동화 = L4의 '측정 게이트'. `/engine`(진단)에 이어 **개선책을 자동 검증**하는 절반.
- **핵심 원칙**: 하네스를 자동으로 바꾸지 않는다 — 판정만 낸다(repfix 교훈: 측정 없는 자동 채택 금지).
- **결과/검증**: `tsc` 통과. judge/summarize 순수함수 유닛 테스트 4/4(효과있음 mech9→0 / 효과없음 3→3 / 역효과 1→10 / 빈결과 안전). 사용: `ROUNDS=5 npx tsx eval/ablate.ts <flag> [task]`.
